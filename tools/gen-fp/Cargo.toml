[package]
name = "gen-fp"
description = "Generates the per-prime unrolled radix field backends of sqisign-verify's round-3 modules (the implementation off x86-64)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
num-bigint = { workspace = true }
num-traits = { workspace = true }
num-integer = { workspace = true }
