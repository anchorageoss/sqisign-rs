name: KAT

on: [push, pull_request]

jobs:
  kat:
    name: KAT vectors (L1/L3/L5)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test -p sqisign-kat --release
