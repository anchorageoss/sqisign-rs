//! SQIsign round-3 level constants (spec Tables 8, 9, 11 and Appendix B),
//! derived from `(p, lambda)`. Needed by the substrate check that runs
//! SQIsign v3 on the ported layers; PRISM defines its own level constants.
//!
//! GENERATED by `tools/gen-precomp`. Do not edit; change the generator.
//! Derivations are documented in PRECOMP.md.

/// NIST level with `lambda = 128` on `p324_3`.
pub mod p324_3 {
    /// Security parameter `lambda`; also the challenge length in bits.
    pub const LAMBDA: u32 = 128;
    /// Response exponent `e_rsp = 1 + ceil((lambda + 2 log2 p) / 4)`.
    pub const RESPONSE_BITS: u32 = 196;
    /// `ceil((e_rsp + 2) / 8)`.
    pub const RESPONSE_BYTES: usize = 25;
    /// `lambda / 8`.
    pub const CHALLENGE_BYTES: usize = 16;
    /// Hash-to-challenge iterations.
    pub const HASH_ITERATIONS: u32 = 1;
    /// Two-adic exponent of the prime.
    pub const TORSION_EVEN_POWER: u32 = 324;
    /// `2 FP_ENCODED_BYTES + 1`.
    pub const PUBLICKEY_BYTES: usize = 83;
    /// `PUBLICKEY_BYTES + 3 FP_ENCODED_BYTES + 4 CHALLENGE_BYTES`.
    pub const SECRETKEY_BYTES: usize = 270;
    /// `2 FP_ENCODED_BYTES + 4 RESPONSE_BYTES + CHALLENGE_BYTES + 2`.
    pub const SIGNATURE_BYTES: usize = 200;
    /// Miller-Rabin rounds for the quaternion layer: `ceil(lambda / 2)`.
    pub const PRIMALITY_NUM_ITER: u32 = 64;
    /// EIBox (spec Algorithm B.2).
    pub const EQUIV_BOUND_COEFF: u32 = 15;
    /// Bit length of `D_mix`.
    pub const DEGREE_NORM_BITS: u32 = 582;
    /// `D_mix = next_prime(p * 2^(2 lambda))`, the secret-key and commitment ideal
    /// norm, little-endian.
    pub const DEG_MIX_LE: [u8; 73] = [
        0x9b, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
        0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
        0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0x2f,
    ];
    /// RICofactor (spec Algorithm B.3): the prime cofactor used by
    /// RandomIdealGivenNorm, little-endian.
    pub const RI_COFACTOR_LE: [u8; 19] = [
        0x55, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x04,
    ];
}

/// NIST level with `lambda = 192` on `p500_27`.
pub mod p500_27 {
    /// Security parameter `lambda`; also the challenge length in bits.
    pub const LAMBDA: u32 = 192;
    /// Response exponent `e_rsp = 1 + ceil((lambda + 2 log2 p) / 4)`.
    pub const RESPONSE_BITS: u32 = 302;
    /// `ceil((e_rsp + 2) / 8)`.
    pub const RESPONSE_BYTES: usize = 38;
    /// `lambda / 8`.
    pub const CHALLENGE_BYTES: usize = 24;
    /// Hash-to-challenge iterations.
    pub const HASH_ITERATIONS: u32 = 1;
    /// Two-adic exponent of the prime.
    pub const TORSION_EVEN_POWER: u32 = 500;
    /// `2 FP_ENCODED_BYTES + 1`.
    pub const PUBLICKEY_BYTES: usize = 129;
    /// `PUBLICKEY_BYTES + 3 FP_ENCODED_BYTES + 4 CHALLENGE_BYTES`.
    pub const SECRETKEY_BYTES: usize = 417;
    /// `2 FP_ENCODED_BYTES + 4 RESPONSE_BYTES + CHALLENGE_BYTES + 2`.
    pub const SIGNATURE_BYTES: usize = 306;
    /// Miller-Rabin rounds for the quaternion layer: `ceil(lambda / 2)`.
    pub const PRIMALITY_NUM_ITER: u32 = 96;
    /// EIBox (spec Algorithm B.2).
    pub const EQUIV_BOUND_COEFF: u32 = 19;
    /// Bit length of `D_mix`.
    pub const DEGREE_NORM_BITS: u32 = 889;
    /// `D_mix = next_prime(p * 2^(2 lambda))`, the secret-key and commitment ideal
    /// norm, little-endian.
    pub const DEG_MIX_LE: [u8; 112] = [
        0x0f, 0x05, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
        0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
        0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
        0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
        0xff, 0xff, 0xff, 0xff, 0xff, 0xaf, 0x01,
    ];
    /// RICofactor (spec Algorithm B.3): the prime cofactor used by
    /// RandomIdealGivenNorm, little-endian.
    pub const RI_COFACTOR_LE: [u8; 28] = [
        0xd9, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x10,
    ];
}

/// NIST level with `lambda = 256` on `p664_17`.
pub mod p664_17 {
    /// Security parameter `lambda`; also the challenge length in bits.
    pub const LAMBDA: u32 = 256;
    /// Response exponent `e_rsp = 1 + ceil((lambda + 2 log2 p) / 4)`.
    pub const RESPONSE_BITS: u32 = 400;
    /// `ceil((e_rsp + 2) / 8)`.
    pub const RESPONSE_BYTES: usize = 51;
    /// `lambda / 8`.
    pub const CHALLENGE_BYTES: usize = 32;
    /// Hash-to-challenge iterations.
    pub const HASH_ITERATIONS: u32 = 1;
    /// Two-adic exponent of the prime.
    pub const TORSION_EVEN_POWER: u32 = 664;
    /// `2 FP_ENCODED_BYTES + 1`.
    pub const PUBLICKEY_BYTES: usize = 169;
    /// `PUBLICKEY_BYTES + 3 FP_ENCODED_BYTES + 4 CHALLENGE_BYTES`.
    pub const SECRETKEY_BYTES: usize = 549;
    /// `2 FP_ENCODED_BYTES + 4 RESPONSE_BYTES + CHALLENGE_BYTES + 2`.
    pub const SIGNATURE_BYTES: usize = 406;
    /// Miller-Rabin rounds for the quaternion layer: `ceil(lambda / 2)`.
    pub const PRIMALITY_NUM_ITER: u32 = 128;
    /// EIBox (spec Algorithm B.2).
    pub const EQUIV_BOUND_COEFF: u32 = 22;
    /// Bit length of `D_mix`.
    pub const DEGREE_NORM_BITS: u32 = 1181;
    /// `D_mix = next_prime(p * 2^(2 lambda))`, the secret-key and commitment ideal
    /// norm, little-endian.
    pub const DEG_MIX_LE: [u8; 148] = [
        ,
    ];
    /// RICofactor (spec Algorithm B.3): the prime cofactor used by
    /// RandomIdealGivenNorm, little-endian.
    pub const RI_COFACTOR_LE: [u8; 36] = [
        0x2b, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0x00, 0x40,
    ];
}
