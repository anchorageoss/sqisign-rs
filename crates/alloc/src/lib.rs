// Dev-only crate hosting the ZeroizingAllocator audit test.
// Excluded from the workspace to avoid #[global_allocator] conflicts.
// Run manually: cargo test --manifest-path crates/alloc/Cargo.toml --release
